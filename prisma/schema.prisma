generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_WITH_SCHEMA")
}

model person {
  id   Int    @id @default(autoincrement())
  name String
  data Bytes?
}
